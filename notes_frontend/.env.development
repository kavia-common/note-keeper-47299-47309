# PUBLIC_INTERFACE
# Fallback development environment variables for Create React App dev server binding.
# CRA reads HOST and PORT to control the dev server bind interface and port.
# These values can be overridden by environment or npm run arguments.
HOST=0.0.0.0
PORT=3000
